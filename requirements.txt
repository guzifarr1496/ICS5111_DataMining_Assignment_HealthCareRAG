# Essential dependencies
numpy
pandas
torch
transformers
sentence-transformers
faiss-cpu
scikit-learn
nltk
tqdm
biopython
streamlit

# Database
sqlite3  # (built-in, not needed in requirements.txt)

# Logging & System Utilities
logging  # (built-in, not needed in requirements.txt)
os       # (built-in, not needed in requirements.txt)
time     # (built-in, not needed in requirements.txt)

# Extra Dependencies (optional)
scipy  # Required for some machine learning tasks
regex  # Improved text processing
